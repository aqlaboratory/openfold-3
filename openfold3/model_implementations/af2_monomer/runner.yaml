general:
  model_preset: model_1
  trainer:
        - num_nodes: 1
        - precision: bf16
        - max_epochs: -1
        - log_every_n_steps: 10
        - flush_logs_ever_n_steps: 10 
        - num_sanity_val_steps: 0
        - reload_dataloaders_every_n_epochs: 1

model:
  template:
    enabled: false